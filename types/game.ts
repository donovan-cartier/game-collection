export interface Game {
    id: number
    name: string
    slug: string
    released: string
    tba: boolean
    background_image: string
    rating: number
    rating_top: number,
    description: string
}